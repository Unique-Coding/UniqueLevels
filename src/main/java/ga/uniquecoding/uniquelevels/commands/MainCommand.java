package ga.uniquecoding.uniquelevels.commands;

import ga.uniquecoding.uniquecore.command.PluginCommandWrapper;
import org.bukkit.command.PluginCommand;

public class MainCommand extends PluginCommandWrapper
{
	public MainCommand(PluginCommand mainCommand)
	{
		super(mainCommand);
	}
}
